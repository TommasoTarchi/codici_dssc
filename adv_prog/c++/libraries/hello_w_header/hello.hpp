/* #pragma once is called header guard, and is needed to
avoid multiple inclusions of functions and classes declared
in the header when compling the main source code */

/* actually the header guard #pragma once is not accepted
by all compilers; to be sure one should use the longer
version of the header guard (can be easily found on the
internet */


#pragma once

void hello();
